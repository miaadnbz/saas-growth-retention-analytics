name: saas_growth_analytics
version: '1.0.0'
config-version: 2

profile: saas_growth_analytics

model-paths: ['models']
macro-paths: ['macros']
target-path: 'target'
clean-targets: ['target', 'dbt_packages']

vars:
  observation_end: '2026-03-31'

models:
  saas_growth_analytics:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: analytics_saas
